events {}

http {

    upstream backend {
        server web_app_1:80;
        server web_app_2:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}